# P0 验收清单

> P0 = 骨架：manifest + 目录 + 消息总线 + i18n 字典骨架 + 设计 token + logger（自检面板 UI 留到 P6）。

## 一、沙盒内已验证（自动）

- [x] `node --test tests/unit` → **22 项全过**
      - i18n：中英键集合/占位符一致、缺键回落、占位符替换
      - errors：错误码归一、限流区分、可重试判定、契约异常识别、envelope 形状
      - logger：级别过滤、token/密码/签名脱敏、递归脱敏、超长折叠、循环引用
      - constants：分页常量分离、默认配置完整、触发默认值为 `icon`
- [x] 全部 16 个源文件 `node --check` 语法通过
- [x] `manifest.json` JSON 合法

## 一之二、沙盒内已验证（准真机：真实源码 + jsdom + 内存 chrome API）

沙盒无法加载真实浏览器（详见 `/data/workspace/tests-real/README.md`），
改用「跑真实源码、mock 宿主环境」的方式补充验证 **18 项全过**：

- [x] 后台 `onInstalled` 初始化：默认配置落盘 / 注册 alarms / 注册右键菜单 / 设置图标行为
- [x] 图标行为智能分流：未登录 `openPanelOnActionClick=false`，已登录 `true`
- [x] 消息总线往返：`ping` / `config_get` / `config_set`；未知消息归一为 `validation_error`
- [x] Popup 渲染 + 与后台往返成功（"SW ready"）
- [x] i18n 运行时切换英文（热更新，不重载页面）
- [x] 配置经 `storage.onChanged` 传播到 content script
- [x] content script 在黑名单站点与普通站点均不抛错
- [x] 日志脱敏：token / 正文不入日志
- [x] 零第三方域名（源码扫描）
- [x] 架构守卫 9 项：core 无平台依赖 / UI 不直调 api / background 无 setInterval / manifest 权限与入口完整性

## 二、仍需你本机加载确认

真机已确认（2026-09-08）：✅ 点图标弹 Popup、✅ 侧边栏打开、✅ Popup 显示 SW ready、✅ 右键显示「存入 flomo」。
以下为剩余项：

在 `chrome://extensions` 加载 `flomo-extension/` 目录后逐项走查：

1. **加载无报错**：扩展卡片无「错误」按钮，Service Worker 显示「有效」
2. **控制台干净**：点 Service Worker 的「检查视图」，应只见 `[flomo:sw] installed`（debug 默认关，可能不显示）
3. ~~**图标行为（未登录）**：点工具栏图标 → 弹出小窗~~ → **真机已确认 ✅**
4. **Side Panel 可用**：浏览器侧边栏菜单里能找到本扩展，打开显示「还没有笔记 · SW ready」
5. **消息总线通**：两个页面都能显示 `SW ready`，说明 `bridge.call('ping')` 往返成功
6. **选项页打开**：点 Popup 里的「设置」按钮 → 打开独立设置页
7. ~~**右键菜单**：在任意网页选中文字右键 → 出现「存入 flomo」~~ → **真机已确认 ✅**
8. ~~**配置热更新**~~ → 沙盒已验证 `storage.onChanged` 可达 content script
9. **深浅色**：把系统切成深色，页面背景应随之切换（`data-theme=system` 生效）— 需真机（仅视觉）
10. ~~**无第三方请求**~~ → 沙盒已用源码扫描验证（P1 后真机再核一次 Network 面板）

## 三、进入 P1 前的必办事项

- [ ] **验证 `PUT /memo` 响应体是否包含 slug**（决定 R13「撤销/删除」能否成立，别无退路时砍掉撤销）
- [ ] 确认 `/memo/updated/` 返回的字段名与类型（游标字段 `latest_updated_at` / `latest_slug`）
- [ ] 确认登录接口的请求参数名（email / password 字段拼写）

> 这三条建议用你自己的账号在浏览器 Network 面板里抓一次真实请求确认，
> 或告诉我你的账号环境，我在 P1 写好客户端后打印完整响应供你核对。
